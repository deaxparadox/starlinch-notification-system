# Postgres, for local docker-compose only — NOT used in production
# (Render provides DATABASE_URL directly for its managed Postgres instance).
POSTGRES_DB=starclinch_notifications
POSTGRES_USER=starclinch
POSTGRES_PASSWORD=localdevpassword
POSTGRES_PORT=5432
