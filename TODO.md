# TODO

## Todo

## In-Progress

## Done
- BUG-20260823-0533 — login/logout/unauthorized pages never had the shared website navbar, since first built - pre-existing, not introduced by the design refresh. Root cause: they lived directly under `frontend/app/`, siblings of the `(website)/` route group folder, not inside it - Next.js only applies a route group's layout to pages physically inside that folder. Found from the user's report that the redesigned login page looked unchanged (no navbar, isolated card); confirmed the gradient/card styling itself was actually rendering correctly via a fresh Playwright screenshot of the real running server, so only the navbar part was a real bug - the rest was a stale browser/dev-server cache on the user's end. Fixed by moving all three pages into `(website)/` (URLs unchanged, route groups don't affect them). Verified: navbar now renders on all three, browser console shows only pre-existing unrelated noise. (spec: docs/specs/BUG-20260823-0533-website-pages-missing-navbar.md) [2026-08-23 05:33]

## Done
- FEAT-20260823-0457 — Design refresh v2, part 5 (final): website pages redesign pass. Applied the full-size gradient-blob backdrop to home, login, logout, and unauthorized. Design call made during implementation: "full hero" was interpreted as the blob size/opacity (per ADR-0003 Decision 2's actual distinction), not a literal copy of the Overview page's headline+subtitle+CTA structure - none of these 4 pages have enough content to carry that structure without looking forced, so each keeps its own existing content sitting on the richer full-size backdrop instead. Added a small `padded?: boolean` prop to `GradientHero` (default `true`, so all 5 earlier call sites are unaffected) so these full-height centered pages could supply their own layout classes instead of the component's header-block padding. Verified with real Playwright screenshots of all 4 pages (including the logout page's brief spinner state, captured by delaying the logout API call so it wouldn't redirect before the screenshot) - all render the same rich full-size blob treatment as the Overview page. This closes design refresh v2 in full: `npm run build` passes clean across all 5 items, and a final lateral sweep found no leftover old-style headers or hardcoded colors outside the deliberately-unchanged Dialog/Switch/Button-secondary-border styling (a reasonable modal/interactive-control distinction, not a leftover inconsistency). (spec: docs/specs/FEAT-20260823-0457-website-pages-redesign-pass.md) [2026-08-23 05:21]
- FEAT-20260823-0456 — Design refresh v2, part 4: admin pages redesign pass. Applied `GradientHero size="reduced"` + the neutral-card/pill language to the trigger table, new-trigger form, template editor, and logs page - matches the approved `trigger-table.html` mockup exactly (small blob, ~28px bold headline, no repeated full hero). Also updated the shared `Table` component (used by both the trigger table and logs page) from ADR-0002's bordered look to Direction D's borderless `surface-warm` rounded card, benefiting both pages from one change (Module 6 lateral fix, not scope creep - same ADR-0003 "whole app" scope). Verified with a real Playwright login against the live backend: screenshotted all 4 pages side by side and confirmed they read as one consistent system (same hero treatment, same sidebar active-state highlighting, same card language) - the actual bar this redesign exists to clear, not just "does each page look fine alone." (spec: docs/specs/FEAT-20260823-0456-admin-pages-redesign-pass.md) [2026-08-23 05:16]
- FEAT-20260823-0455 — Design refresh v2, part 3: new admin Overview page. Full `GradientHero` + greeting headline + 3 real stat cards + recent-activity list, wired to FEAT-20260823-0454's live `/api/admin/stats/` endpoint (no mock data). Moved the old trigger table from `/admin` to a new `/admin/triggers` route so Overview could take `/admin` as the sidebar's first item and default landing; fixed the 3 links/redirects that expected the trigger table at `/admin` (new-trigger's post-create redirect and Cancel link, template editor's Cancel link) to point at `/admin/triggers` instead - the website's admin-nav link needed no change since landing on Overview first is the intended behavior. Verified with a real Playwright-driven login against the live local backend (not a curl auth bypass): Overview renders real numbers matching the database, the moved trigger table still works identically at its new URL (including the FEAT-20260823-0453 pill/card styling carrying over automatically), and the sidebar highlights the correct item on each page. Console errors seen during verification (OneSignal production-domain restriction, a couple of 401s) are pre-existing dev-environment noise unrelated to this change, confirmed by their unchanged presence on the untouched login page. (spec: docs/specs/FEAT-20260823-0455-admin-overview-page.md) [2026-08-23 05:09]
- FEAT-20260823-0454 — Design refresh v2, part 2: backend stats endpoint. New `GET /api/admin/stats/` (`IsAdminUser`) returns sent/failed counts for today (`timezone.localdate()`-bounded), active/total trigger counts, and the 5 most recent `NotificationLog` rows - reuses the existing `NotificationLogSerializer` unchanged. Test-sends (`is_test=True`) are excluded from every count and from `recent`, since a "test" button click isn't real activity. Verified live against the real local backend: unauthenticated request gets 401, numbers match real `NotificationLog` rows for a known admin account, and an isolated before/after check (no HTTP round-trip in between, to rule out concurrent real activity from the user's own live testing) confirmed adding an `is_test=True` row leaves every count unchanged. (spec: docs/specs/FEAT-20260823-0454-admin-stats-endpoint.md) [2026-08-23 05:03]
- FEAT-20260823-0453 — Design refresh v2, part 1: design system v2 foundation. New Tailwind tokens for the "Direction D" visual language confirmed with the user via visual-companion mockups (white background, near-black foreground/primary, new `surface-warm` neutral card color, 4 blob hues, soft-tint channel badge backgrounds), dark mode dropped entirely (`@media (prefers-color-scheme: dark)` block removed - confirmed no component used Tailwind's `dark:` prefix, so this was the complete removal). `Card` and `Badge` updated to the new pill/neutral-card look; new `GradientHero` component built (size: "full" | "reduced", exact blob geometry taken from the approved mockups) - the shared piece every later item in this redesign reuses. Verified: `npm run build` passes clean, and a throwaway test route rendered both `GradientHero` sizes plus `Card`/`ChannelBadge`/`StatusBadge` together, screenshotted via headless Chrome, and visually matched the approved mockups closely before being deleted. (spec: docs/specs/FEAT-20260823-0453-design-system-v2-foundation.md) [2026-08-23 05:00]
- BUG-20260823-0403 — Web Push always notified only one of a user's devices, permanently, regardless of which device actually triggered the notification. Root cause: `_resolve_recipient()` in `backend/notifications/services.py` called `user.pushsubscription_set.first()` for the webpush channel, which deterministically returned whichever device subscribed first ever for that account (Django orders an unordered `.first()` by primary key) - every other subscribed device was silently ignored forever. Found by the user testing on Windows, Linux, and mobile: only the account's oldest-subscribed device (Linux) ever received a notification, no matter which device logged in. Also verified (via context7, OneSignal's current Web SDK docs) that the frontend's `OneSignal.User.PushSubscription.id` is genuinely per-device, ruling out a person-level-ID theory before committing to the backend fix. Fixed: `_resolve_recipients()` now returns every active `PushSubscription` for the user, and `_send_one`/`_send_to_one_recipient` send + log one attempt per device independently, so one stale device failing can't hide or block another device succeeding; overall channel status is "sent" if at least one device received it. Verified live against real OneSignal credentials: a real device + a deliberately fake device on the same account both logged their own row (real one "sent", fake one "failed" with its own error, real push notification actually delivered), overall result still "sent"; a user with zero subscriptions still logs exactly one clean "no recipient" row, not one per nonexistent device. (spec: docs/specs/BUG-20260823-0403-webpush-multi-device-fanout.md) [2026-08-23 04:05]
- BUG-20260823-0329 — README's "How to log in as admin" and "Deployment" sections told you to create the first Django admin via Render's Shell tab, but Render's free tier has no Shell/SSH access (and no one-off Jobs either) — that instruction could not actually be followed once deployed. Fixed: `docker-entrypoint.sh` now creates the admin account itself on startup, idempotently, when three optional env vars (`DJANGO_SUPERUSER_USERNAME`/`EMAIL`/`PASSWORD`) are set (added to `render.yaml` and `backend/.env.example`); no admin-panel self-signup UI, since a first admin has to exist before the admin panel can be reached at all, and further admins get created from inside the admin panel by that first admin. README corrected to match (spec: docs/specs/BUG-20260823-0329-render-superuser-bootstrap.md). Verified live via `docker compose run` against the real entrypoint script (not the compose override): fresh username creates and logs it, existing username skips and logs that too, vars left unset behave exactly as before with no new output. [2026-08-23 03:32]
- FEAT-20260823-0255 — Vercel project config: frontend/vercel.json (framework declaration + ignoreCommand to skip rebuilds when only backend/ changes), the Vercel-side counterpart to render.yaml so both platforms' deploy scope is enforced by a committed config file, not just a dashboard setting (spec: docs/specs/FEAT-20260823-0255-vercel-config.md). Verified ignoreCommand's exit-code semantics against Vercel's current docs before writing it (exit 0 skips, exit 1 continues - the opposite of a naive guess). [2026-08-23 02:55]

## Done
- FEAT-20260822-1219 — README: admin login instructions, triggers built, env vars needed, known friction points, Task D plain-language answers (spec: docs/specs/FEAT-20260822-1219-readme.md). Written with everything learned this session: OneSignal verified fully working, Postmark's work-email signup requirement + approval-lifts-restriction behavior, WhatsApp's 5-number sandbox cap + OTP verification + Business Portfolio age gate, a "what's actually done" status table, and an explicit Submission section (GitHub repo + live URL placeholders) mapping directly to the assignment's "what to submit" checklist. [2026-08-23 01:08]
- BUG-20260823-0012 — Two real gaps found by user: (1) no way to navigate from the public website to the admin panel - no admin link existed anywhere on the site, so a staff user had to manually type /admin; the website and admin sections felt like two disconnected apps with no linkage between them. (2) The BUG-20260822-2340 max-width fix only constrained the outer content container - form-heavy pages (template editor, new-trigger) still looked lopsided because their own inner content used a narrower max-w-sm/max-w-md left-aligned inside the wider box, leaving a large dead zone on the right. Fixed: added a conditional "Admin" link to the website nav (staff only) and a "View site" link in the admin top bar for the reverse direction, and made the template-editor/new-trigger pages each a single self-contained, centered max-w-2xl column instead of a narrow form floating inside a wider one. Verified at the same wide viewport: admin link only shows for staff, both pages read as properly proportioned centered columns now. [2026-08-23 00:12]
- BUG-20260822-2340 — Admin shell (FEAT-20260822-2249 through 2252) had no content max-width, so every admin page's content stretched edge-to-edge on wide screens leaving tables/empty-states isolated in unused whitespace (and the trigger table's own leftover `max-w-3xl` made it inconsistently narrower than everything else around it), and the sidebar had no collapse control. User called this out directly as not senior-level UI/UX work. Fixed: added a shared `AdminTopBar` (with the sidebar-collapse toggle) and a `max-w-5xl` container wrapping all admin page content in `(admin)/layout.tsx`, removed the redundant `max-w-3xl` on the trigger table, and made the sidebar collapsible to an icon-only rail with state persisted via localStorage (using `useSyncExternalStore`, not a setState-in-effect). Verified at the same wide viewport that exposed the issue: content now sits in a sensible constrained width, collapse toggle works and persists across reload. [2026-08-22 23:48]
- FEAT-20260822-2252 — Website pages redesign: home, login, logout, unauthorized restyled with the new design system (spec: docs/specs/FEAT-20260822-2252-website-pages-redesign.md) [2026-08-22 23:33]
- FEAT-20260822-2251 — New Logs page: /admin/logs using the existing GET /api/admin/logs/ endpoint (spec: docs/specs/FEAT-20260822-2251-logs-page.md) [2026-08-22 23:24]
- FEAT-20260822-2250 — Admin screens redesign: trigger table, new-trigger form, template editor rebuilt on the new component system (spec: docs/specs/FEAT-20260822-2250-admin-screens-redesign.md) [2026-08-22 23:18]
- FEAT-20260822-2249 — Admin shell: sidebar layout + nav for the (admin) section (spec: docs/specs/FEAT-20260822-2249-admin-shell.md) [2026-08-22 23:08]
- FEAT-20260822-2248 — Design system foundation: Tailwind theme tokens (light/dark "Signal" palette), typography (Geist actually applied), reusable components (Button, Input, Switch, Badge, Card, Table, Dialog, Toast, EmptyState, Skeleton), icons (lucide-react) (spec: docs/specs/FEAT-20260822-2248-design-system-foundation.md) [2026-08-22 23:03]
- FEAT-20260822-1218 — Deployment prep: fixed Dockerfile to bind Render's $PORT, added an entrypoint script (migrate + exec gunicorn for correct signal handling), whitenoise for static files, and a render.yaml blueprint. Scope changed after checking with user: they'll create the Render/Vercel accounts and do the actual deploy + createsuperuser themselves, using instructions in FEAT-20260822-1219's README (spec: docs/specs/FEAT-20260822-1218-deployment-prep.md) [2026-08-22 20:29]
- FEAT-20260822-1217 — Admin panel UI: trigger x channel table, template editor, toggles, test-send button, unauthorized page (spec: docs/specs/FEAT-20260822-1217-admin-panel-ui.md) [2026-08-22 20:15]
- FEAT-20260822-1216 — Website pages: login/logout pages that fire triggers, post-login Web Push subscribe flow (OneSignal) (spec: docs/specs/FEAT-20260822-1216-website-pages.md) [2026-08-22 19:56]
- FEAT-20260822-1215 — Frontend foundation: Next.js app skeleton with (website) and (admin) route groups, shared API client, auth handling (spec: docs/specs/FEAT-20260822-1215-frontend-foundation.md) [2026-08-22 19:34]
- FEAT-20260822-1214 — Admin API: CRUD for triggers/templates (upsert), toggle on/off, test-send, logs endpoints, IsAdminUser enforcement, CORS config (spec: docs/specs/FEAT-20260822-1214-admin-api.md) [2026-08-22 18:51]
- FEAT-20260822-1213 — Notifications core: Trigger/Template/NotificationLog/PushSubscription models, WhatsApp/Postmark/OneSignal adapters (ports & adapters pattern), fire_trigger() service layer (spec: docs/specs/FEAT-20260822-1213-notifications-core.md) [2026-08-22 18:47]
- FEAT-20260822-1212 — Backend foundation: Django project, custom User model, JWT login/logout/refresh (access token in body, refresh token in httpOnly cookie), Docker + docker-compose for local dev (spec: docs/specs/FEAT-20260822-1212-backend-foundation.md) [2026-08-22 18:24]
- BUG-20260822-1234 — Refresh token not invalidated on logout/refresh (session fixation risk), and logout's cookie-clear used Django's delete_cookie() which has no `secure` param and silently fails to clear a SameSite=None cookie in browsers. Found by automated security review of FEAT-20260822-1212's commit. Fixed: added rest_framework_simplejwt.token_blacklist, refresh tokens now rotate + blacklist the used token on every /refresh/ and /logout/ call, and cookie clearing uses set_cookie(max_age=0, ...) with matching flags instead of delete_cookie(). [2026-08-22 18:34]
- BUG-20260822-2000 — POST /api/webpush/subscribe/ (FEAT-20260822-1216) let any authenticated user silently reassign an existing PushSubscription away from its real owner just by supplying that user's onesignal_player_id, since it trusted client-supplied input and used update_or_create unconditionally. Found by automated security review of FEAT-20260822-1216's commit. Fixed: reject with 409 when the player_id already belongs to a different user; creating a new one or re-registering one this same user already owns still works. Verified via curl: cross-user steal attempt now 409s, same-user re-registration still 204s. [2026-08-22 20:00]
- BUG-20260822-2102 — docker-compose.yml's db healthcheck ran `pg_isready -U $POSTGRES_USER` with no `-d` flag, so it probed a database named after the user ("starclinch") instead of the actual app database ("starclinch_notifications") - that database never existed, so Postgres logged a "FATAL: database does not exist" error every 5 seconds forever. Found by user running `docker compose up --build` themselves. The app itself worked fine throughout (Django connects using the correct DATABASE_URL) - this was log noise from a wrong healthcheck, not a functional break. Fixed: added `-d $POSTGRES_DB`. Verified: db reports healthy with a single successful "accepting connections" check, no more repeating FATAL lines. [2026-08-22 21:02]
- BUG-20260822-2226 — FEAT-20260822-1216's OneSignal Web Push integration was incomplete: the JS-side init/subscribe code existed but the actual service worker file the SDK needs (`OneSignalSDKWorker.js`, served from the site root) was never created. Never caught earlier because no real OneSignal credentials existed yet to exercise the flow. Found while helping the user set up a real OneSignal sandbox account and test it live. Fixed: added `frontend/public/OneSignalSDKWorker.js` (one line, `importScripts` pulling OneSignal's v16 CDN worker script - their own documented self-hosting pattern). Verified with the user's real OneSignal account: browser prompted for permission, a real PushSubscription row was created, and two real "sent" webpush NotificationLog rows exist with an actual browser push notification received. [2026-08-22 22:26]
