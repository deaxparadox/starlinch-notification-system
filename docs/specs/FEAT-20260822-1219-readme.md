# FEAT-20260822-1219 — README

Branch: `main` (base: `main`)

## What's being built
The single root `README.md` the assignment asks for, covering everything it explicitly requires
plus the deployment walkthrough (since the user will execute that themselves using `render.yaml`
from FEAT-20260822-1218 and Vercel's standard flow).

## Current state (verified)
No root `README.md` exists yet. `frontend/README.md` is create-next-app's default boilerplate —
gets replaced with a one-line pointer to the root README so there's a single source of truth,
not two READMEs telling a different story.

## Sections (mapping directly to the assignment's explicit requirements + Module 7's close-out list)
1. **What this is** — one paragraph, links the assignment doc.
2. **How to log in as admin** — exact `createsuperuser` command to run via Render's Shell tab
   against the live database, once deployed.
3. **Which triggers were built** — Login and Logout (event-based), with the explicit,
   already-decided reason inactivity triggers (1 day/1 week) are out of scope: Render's free tier
   has no scheduler/cron service (ADR-0001, Decision 3).
4. **Architecture at a glance** — short version of ADR-0001's decisions (sync threaded send,
   scoped ports/adapters), linking to the full ADR rather than repeating it.
5. **Env vars needed** — mirrors `backend/.env.example`, `frontend/.env.local.example`, and root
   `.env.example`, grouped by required-vs-optional per Module 4's distinction already encoded in
   the settings files.
6. **Local development** — `docker-compose up` path and the plain-venv path (both actually used
   during this build, not just theorized).
7. **Deployment** — step-by-step: Render Blueprint (`render.yaml`) for the backend + Postgres,
   Vercel import for the frontend, which env vars to fill in on each, then `createsuperuser` via
   Render's Shell tab.
8. **Known friction points** — Postmark's ~24h account approval, WhatsApp's Meta
   business-account-age gate, OneSignal's incognito-mode subscribe limitation, Render's free
   Postgres expiring after a time limit — all already known facts from the discussion docs, not
   new claims.
9. **Task D answers** (plain language, per the assignment's own wording) — what is a trigger + 3
   examples beyond login, the three channels, why templates live in the admin panel instead of
   Postmark/WhatsApp's own sites, what Web Push is.
10. **GitHub repo / live URLs** — repo link filled in now; Render/Vercel URLs left as explicit
    placeholders for the user to fill in after they deploy (not fabricated).

## Verification plan
Read back against the assignment PDF's "What to submit" and "Checklist" sections point by point to
confirm nothing required is missing before considering this done.
